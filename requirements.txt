# Requirements for Vercel deployment
# Note: PyTorch CPU-only is used to reduce deployment size

# PyTorch (CPU-only for serverless)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
torchaudio

# Web framework
fastapi
uvicorn[standard]

# Gradio and dependencies
gradio>=4.0.0,<5.0.0
huggingface-hub<0.24

# Data/ML
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
scikit-learn>=1.0.0
